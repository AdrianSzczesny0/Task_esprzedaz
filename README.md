# Task_esprzedaz – Test Suite

This project contains automated tests for the **Sellingo** system.  
It uses **TestNG** and **Maven** as the test framework and build tool.

---

## ✅ Running Tests

To run all tests using Maven:

```bash
mvn test -Denv=demo
